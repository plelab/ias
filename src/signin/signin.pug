doctype html

html
    head
        include ../public/jade/header
        script(src="/signin/signin.js")

    body
        div.local-login
            div
                label User ID :
                input(type="text", name="user_id")
            div
                label Password :
                input(type="password", name="passwd")
            div
                button(onClick="signin()") Login

        form(action="/api/sampleFacebookPassport", method="POST")
            input(type="submit", value="Facebook Login")